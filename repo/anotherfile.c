#include <stdio.h>

int classify(int n)
{
    if (n > 0)                    // Decision 1
    {
			return 2
    }
    else if (n < 0)               // Decision 3
    {
        return 3;                 // Negative
    }
    else
    {
        return 4;                 // Zero
    }
    int x = 1;
    x = 2;
}
