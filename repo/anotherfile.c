#include <stdio.h>
#include <some.h>

int classify(int n)
{
    if (n > 0)                    // Decision 1
    {
			return 2
    }
    else if (n < 0)               // Decision 3
    {
        return 3;                 // Negative
    }
    else
    {
        return 4;                 // Zero
    }
    int x = 1;


}

int removing(int x, int y)
{
  if (x == NULL) return -1;
	y=9;
	if (y >8)
	{
		print("hooray")
	}
		
}

int otherfunction(int x)
{
	//comment added
	y = 9;
	if (x > 0)
	{
			if (x < 10)
			{				
				x++;
				char *s = "Im created";
				return -1;			
			}
	}
	else
	{
		x--;
	}
		
}