#include <stdio.h>

int classify(int n)
{
    if (n > 0)                    // Decision 1
    {
			return 2
    }
    else if (n < 0)               // Decision 3
    {
        return 3;                 // Negative
    }
    else
    {
        return 4;                 // Zero
    }
    int x = 1;

    if (x != 4)
    {
    		//do nothing
    }
}

int removing(int x, int y)
{
  if (x == NULL) return -1;
	y=9;
	
}

int otherfunction(int x)
{
	//comment added
	y = 9;
	if (x > 0)
	{
			if (x < 10)
			{				
				x++;
				char *s = "Im created";
				return -1;			
			}
	}
	else
	{
		x--;
	}
		
}